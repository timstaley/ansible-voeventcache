- hosts: test


  roles:
    - timstaley.base
    - role: kamaln7.swapfile
      sudo: yes
    - { role: ../../voeventdb,
        voeventdb_domain: "localhost",
        voeventdb_rest_enabled: false
        }
