---
# vars file for voeventdb
voeventdb_users:
  - "{{voeventdb_user}}"
  - "{{voeventdb_ro_user}}"


voeventdb_db_url: "postgresql://{{ voeventdb_user }}:{{ voeventdb_user }}@localhost/{{ voeventdb_db }}"
voeventdb_db_ro_url: "postgresql://{{ voeventdb_ro_user }}:{{ voeventdb_ro_user }}@localhost/{{ voeventdb_db }}"
